services:
  - type: web
    name: crunchyroll-web-checker
    env: python
    region: singapore  # or "oregon", "frankfurt" — change as needed
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 app.py
    envVars:
      - key: FLASK_ENV
        value: production
